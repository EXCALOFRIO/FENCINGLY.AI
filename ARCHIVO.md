# Archived branch - previous version of FENCINGLY.AI

This branch is a **snapshot of the repository as it was before 2026-09-11**, kept for reference only.

**All video files were removed** (`.mp4`, `.avi`, `.gif`, `.zip` bundles of clips): they were several
gigabytes, they are broadcast footage that is not ours to redistribute, and nothing here depends on
them. Everything else from the old tree is kept: the Colab notebook, the dissertation PDF, the FIE
data, the old model weights and the old sources.

The current work lives on `main`.
